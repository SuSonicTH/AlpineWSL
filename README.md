# AlpineWSL
a simple bash script to download and install a minimal Alpine Linux distribution in WSL

to install just put the script `AlpineInstall.sh` in an empty directory and execute in a bash in windows
